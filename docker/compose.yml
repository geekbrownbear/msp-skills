# GENERATED by tools/maintainer/build_compose.py - DO NOT EDIT.
# Regenerate: python3 tools/maintainer/build_compose.py
# Gated by:   python3 tools/maintainer/check_compose.py
#
# One service per connector, each behind its own Compose profile, so
# `docker compose --profile halopsa up -d` runs exactly one.
#
# No connector publishes a port. Do not add `ports:` to a service here:
# the MCP servers have no authentication of their own, so publishing one
# puts a fully privileged, unauthenticated server on the LAN.
# check_compose.py fails on it.

name: msp-skills

services:
  abnormal:
    profiles: [abnormal]
    image: "${MSP_IMAGE_REPO:-msp-skills}/abnormal:${ABNORMAL_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: abnormal
        CLI_BINARY: abnormal-cli
        MCP_BINARY: abnormal-mcp
        VERSION: "${ABNORMAL_VERSION:-0.1.1}"
    container_name: msp-abnormal
    env_file:
      - env/abnormal.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-abnormal-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  acronis:
    profiles: [acronis]
    image: "${MSP_IMAGE_REPO:-msp-skills}/acronis:${ACRONIS_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: acronis
        CLI_BINARY: acronis-cli
        MCP_BINARY: acronis-mcp
        VERSION: "${ACRONIS_VERSION:-0.1.1}"
    container_name: msp-acronis
    env_file:
      - env/acronis.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-acronis-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  action1:
    profiles: [action1]
    image: "${MSP_IMAGE_REPO:-msp-skills}/action1:${ACTION1_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: action1
        CLI_BINARY: action1-cli
        MCP_BINARY: action1-mcp
        VERSION: "${ACTION1_VERSION:-0.1.1}"
    container_name: msp-action1
    env_file:
      - env/action1.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-action1-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  afi:
    profiles: [afi]
    image: "${MSP_IMAGE_REPO:-msp-skills}/afi:${AFI_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: afi
        CLI_BINARY: afi-cli
        MCP_BINARY: afi-mcp
        VERSION: "${AFI_VERSION:-0.1.1}"
    container_name: msp-afi
    env_file:
      - env/afi.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-afi-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  appdirect:
    profiles: [appdirect]
    image: "${MSP_IMAGE_REPO:-msp-skills}/appdirect:${APPDIRECT_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: appdirect
        CLI_BINARY: appdirect-cli
        MCP_BINARY: appdirect-mcp
        VERSION: "${APPDIRECT_VERSION:-0.1.1}"
    container_name: msp-appdirect
    env_file:
      - env/appdirect.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-appdirect-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  atera:
    profiles: [atera]
    image: "${MSP_IMAGE_REPO:-msp-skills}/atera:${ATERA_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: atera
        CLI_BINARY: atera-cli
        MCP_BINARY: atera-mcp
        VERSION: "${ATERA_VERSION:-0.1.1}"
    container_name: msp-atera
    env_file:
      - env/atera.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-atera-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  autotask:
    profiles: [autotask]
    image: "${MSP_IMAGE_REPO:-msp-skills}/autotask:${AUTOTASK_VERSION:-0.1.2}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: autotask
        CLI_BINARY: autotask-cli
        MCP_BINARY: autotask-mcp
        VERSION: "${AUTOTASK_VERSION:-0.1.2}"
    container_name: msp-autotask
    env_file:
      - env/autotask.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-autotask-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  auvik:
    profiles: [auvik]
    image: "${MSP_IMAGE_REPO:-msp-skills}/auvik:${AUVIK_VERSION:-0.2.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: auvik
        CLI_BINARY: auvik-cli
        MCP_BINARY: auvik-mcp
        VERSION: "${AUVIK_VERSION:-0.2.0}"
    container_name: msp-auvik
    env_file:
      - env/auvik.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-auvik-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  aws-billing:
    profiles: [aws-billing]
    image: "${MSP_IMAGE_REPO:-msp-skills}/aws-billing:${AWS_BILLING_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: aws-billing
        CLI_BINARY: aws-billing-cli
        MCP_BINARY: aws-billing-mcp
        VERSION: "${AWS_BILLING_VERSION:-0.1.0}"
    container_name: msp-aws-billing
    env_file:
      - env/aws-billing.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-aws-billing-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  axcient:
    profiles: [axcient]
    image: "${MSP_IMAGE_REPO:-msp-skills}/axcient:${AXCIENT_VERSION:-0.2.9}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: axcient
        CLI_BINARY: axcient-cli
        MCP_BINARY: axcient-mcp
        VERSION: "${AXCIENT_VERSION:-0.2.9}"
    container_name: msp-axcient
    env_file:
      - env/axcient.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-axcient-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  betterstack:
    profiles: [betterstack]
    image: "${MSP_IMAGE_REPO:-msp-skills}/betterstack:${BETTERSTACK_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: betterstack
        CLI_BINARY: betterstack-cli
        MCP_BINARY: betterstack-mcp
        VERSION: "${BETTERSTACK_VERSION:-0.1.1}"
    container_name: msp-betterstack
    env_file:
      - env/betterstack.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-betterstack-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  blumira:
    profiles: [blumira]
    image: "${MSP_IMAGE_REPO:-msp-skills}/blumira:${BLUMIRA_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: blumira
        CLI_BINARY: blumira-cli
        MCP_BINARY: blumira-mcp
        VERSION: "${BLUMIRA_VERSION:-0.1.1}"
    container_name: msp-blumira
    env_file:
      - env/blumira.env
    environment:
      HOME: /data
    user: "10001:10001"
    volumes:
      - msp-blumira-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  cipp:
    profiles: [cipp]
    image: "${MSP_IMAGE_REPO:-msp-skills}/cipp:${CIPP_VERSION:-0.1.2}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: cipp
        CLI_BINARY: cipp-cli
        MCP_BINARY: cipp-mcp
        VERSION: "${CIPP_VERSION:-0.1.2}"
    container_name: msp-cipp
    env_file:
      - env/cipp.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-cipp-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  connectwise-automate:
    profiles: [connectwise-automate]
    image: "${MSP_IMAGE_REPO:-msp-skills}/connectwise-automate:${CONNECTWISE_AUTOMATE_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: connectwise-automate
        CLI_BINARY: connectwise-automate-cli
        MCP_BINARY: connectwise-automate-mcp
        VERSION: "${CONNECTWISE_AUTOMATE_VERSION:-0.1.0}"
    container_name: msp-connectwise-automate
    env_file:
      - env/connectwise-automate.env
    environment:
      HOME: /data
    user: "10001:10001"
    volumes:
      - msp-connectwise-automate-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  connectwise-control:
    profiles: [connectwise-control]
    image: "${MSP_IMAGE_REPO:-msp-skills}/connectwise-control:${CONNECTWISE_CONTROL_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: connectwise-control
        CLI_BINARY: connectwise-control-cli
        MCP_BINARY: connectwise-control-mcp
        VERSION: "${CONNECTWISE_CONTROL_VERSION:-0.1.1}"
    container_name: msp-connectwise-control
    env_file:
      - env/connectwise-control.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-connectwise-control-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  connectwise-manage:
    profiles: [connectwise-manage]
    image: "${MSP_IMAGE_REPO:-msp-skills}/connectwise-manage:${CONNECTWISE_MANAGE_VERSION:-0.1.4}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: connectwise-manage
        CLI_BINARY: connectwise-manage-cli
        MCP_BINARY: connectwise-manage-mcp
        VERSION: "${CONNECTWISE_MANAGE_VERSION:-0.1.4}"
    container_name: msp-connectwise-manage
    env_file:
      - env/connectwise-manage.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-connectwise-manage-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  cork:
    profiles: [cork]
    image: "${MSP_IMAGE_REPO:-msp-skills}/cork:${CORK_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: cork
        CLI_BINARY: cork-cli
        MCP_BINARY: cork-mcp
        VERSION: "${CORK_VERSION:-0.1.0}"
    container_name: msp-cork
    env_file:
      - env/cork.env
    environment:
      HOME: /data
    user: "10001:10001"
    volumes:
      - msp-cork-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  cove:
    profiles: [cove]
    image: "${MSP_IMAGE_REPO:-msp-skills}/cove:${COVE_VERSION:-0.1.4}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: cove
        CLI_BINARY: cove-cli
        MCP_BINARY: cove-mcp
        VERSION: "${COVE_VERSION:-0.1.4}"
    container_name: msp-cove
    env_file:
      - env/cove.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-cove-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  crowdstrike:
    profiles: [crowdstrike]
    image: "${MSP_IMAGE_REPO:-msp-skills}/crowdstrike:${CROWDSTRIKE_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: crowdstrike
        CLI_BINARY: crowdstrike-cli
        MCP_BINARY: crowdstrike-mcp
        VERSION: "${CROWDSTRIKE_VERSION:-0.1.1}"
    container_name: msp-crowdstrike
    env_file:
      - env/crowdstrike.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-crowdstrike-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  datto-bcdr:
    profiles: [datto-bcdr]
    image: "${MSP_IMAGE_REPO:-msp-skills}/datto-bcdr:${DATTO_BCDR_VERSION:-0.1.2}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: datto-bcdr
        CLI_BINARY: datto-bcdr-cli
        MCP_BINARY: datto-bcdr-mcp
        VERSION: "${DATTO_BCDR_VERSION:-0.1.2}"
    container_name: msp-datto-bcdr
    env_file:
      - env/datto-bcdr.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-datto-bcdr-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  datto-rmm:
    profiles: [datto-rmm]
    image: "${MSP_IMAGE_REPO:-msp-skills}/datto-rmm:${DATTO_RMM_VERSION:-0.1.2}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: datto-rmm
        CLI_BINARY: datto-rmm-cli
        MCP_BINARY: datto-rmm-mcp
        VERSION: "${DATTO_RMM_VERSION:-0.1.2}"
    container_name: msp-datto-rmm
    env_file:
      - env/datto-rmm.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-datto-rmm-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  domotz:
    profiles: [domotz]
    image: "${MSP_IMAGE_REPO:-msp-skills}/domotz:${DOMOTZ_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: domotz
        CLI_BINARY: domotz-cli
        MCP_BINARY: domotz-mcp
        VERSION: "${DOMOTZ_VERSION:-0.1.1}"
    container_name: msp-domotz
    env_file:
      - env/domotz.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-domotz-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  gradient:
    profiles: [gradient]
    image: "${MSP_IMAGE_REPO:-msp-skills}/gradient:${GRADIENT_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: gradient
        CLI_BINARY: gradient-cli
        MCP_BINARY: gradient-mcp
        VERSION: "${GRADIENT_VERSION:-0.1.1}"
    container_name: msp-gradient
    env_file:
      - env/gradient.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-gradient-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  halopsa:
    profiles: [halopsa]
    image: "${MSP_IMAGE_REPO:-msp-skills}/halopsa:${HALOPSA_VERSION:-0.2.12}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: halopsa
        CLI_BINARY: halopsa-cli
        MCP_BINARY: halopsa-mcp
        VERSION: "${HALOPSA_VERSION:-0.2.12}"
    container_name: msp-halopsa
    env_file:
      - env/halopsa.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-halopsa-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  hubspot:
    profiles: [hubspot]
    image: "${MSP_IMAGE_REPO:-msp-skills}/hubspot:${HUBSPOT_VERSION:-0.1.3}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: hubspot
        CLI_BINARY: hubspot-cli
        MCP_BINARY: hubspot-mcp
        VERSION: "${HUBSPOT_VERSION:-0.1.3}"
    container_name: msp-hubspot
    env_file:
      - env/hubspot.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-hubspot-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  hudu:
    profiles: [hudu]
    image: "${MSP_IMAGE_REPO:-msp-skills}/hudu:${HUDU_VERSION:-0.1.6}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: hudu
        CLI_BINARY: hudu-cli
        MCP_BINARY: hudu-mcp
        VERSION: "${HUDU_VERSION:-0.1.6}"
    container_name: msp-hudu
    env_file:
      - env/hudu.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-hudu-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  huntress:
    profiles: [huntress]
    image: "${MSP_IMAGE_REPO:-msp-skills}/huntress:${HUNTRESS_VERSION:-0.1.2}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: huntress
        CLI_BINARY: huntress-cli
        MCP_BINARY: huntress-mcp
        VERSION: "${HUNTRESS_VERSION:-0.1.2}"
    container_name: msp-huntress
    env_file:
      - env/huntress.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-huntress-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  immybot:
    profiles: [immybot]
    image: "${MSP_IMAGE_REPO:-msp-skills}/immybot:${IMMYBOT_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: immybot
        CLI_BINARY: immybot-cli
        MCP_BINARY: immybot-mcp
        VERSION: "${IMMYBOT_VERSION:-0.1.0}"
    container_name: msp-immybot
    env_file:
      - env/immybot.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-immybot-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  itglue:
    profiles: [itglue]
    image: "${MSP_IMAGE_REPO:-msp-skills}/itglue:${ITGLUE_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: itglue
        CLI_BINARY: itglue-cli
        MCP_BINARY: itglue-mcp
        VERSION: "${ITGLUE_VERSION:-0.1.1}"
    container_name: msp-itglue
    env_file:
      - env/itglue.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-itglue-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  kaseya-bms:
    profiles: [kaseya-bms]
    image: "${MSP_IMAGE_REPO:-msp-skills}/kaseya-bms:${KASEYA_BMS_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: kaseya-bms
        CLI_BINARY: kaseya-bms-cli
        MCP_BINARY: kaseya-bms-mcp
        VERSION: "${KASEYA_BMS_VERSION:-0.1.1}"
    container_name: msp-kaseya-bms
    env_file:
      - env/kaseya-bms.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-kaseya-bms-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  knowbe4:
    profiles: [knowbe4]
    image: "${MSP_IMAGE_REPO:-msp-skills}/knowbe4:${KNOWBE4_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: knowbe4
        CLI_BINARY: knowbe4-cli
        MCP_BINARY: knowbe4-mcp
        VERSION: "${KNOWBE4_VERSION:-0.1.1}"
    container_name: msp-knowbe4
    env_file:
      - env/knowbe4.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-knowbe4-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  levelio:
    profiles: [levelio]
    image: "${MSP_IMAGE_REPO:-msp-skills}/levelio:${LEVELIO_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: levelio
        CLI_BINARY: levelio-cli
        MCP_BINARY: levelio-mcp
        VERSION: "${LEVELIO_VERSION:-0.1.1}"
    container_name: msp-levelio
    env_file:
      - env/levelio.env
    environment:
      HOME: /data
    user: "10001:10001"
    volumes:
      - msp-levelio-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  liongard:
    profiles: [liongard]
    image: "${MSP_IMAGE_REPO:-msp-skills}/liongard:${LIONGARD_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: liongard
        CLI_BINARY: liongard-cli
        MCP_BINARY: liongard-mcp
        VERSION: "${LIONGARD_VERSION:-0.1.1}"
    container_name: msp-liongard
    env_file:
      - env/liongard.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-liongard-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  maxio:
    profiles: [maxio]
    image: "${MSP_IMAGE_REPO:-msp-skills}/maxio:${MAXIO_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: maxio
        CLI_BINARY: maxio-cli
        MCP_BINARY: maxio-mcp
        VERSION: "${MAXIO_VERSION:-0.1.0}"
    container_name: msp-maxio
    env_file:
      - env/maxio.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-maxio-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  microsoft-graph:
    profiles: [microsoft-graph]
    image: "${MSP_IMAGE_REPO:-msp-skills}/microsoft-graph:${MICROSOFT_GRAPH_VERSION:-0.2.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: microsoft-graph
        CLI_BINARY: microsoft-graph-cli
        MCP_BINARY: microsoft-graph-mcp
        VERSION: "${MICROSOFT_GRAPH_VERSION:-0.2.0}"
    container_name: msp-microsoft-graph
    env_file:
      - env/microsoft-graph.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-microsoft-graph-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  mspbots:
    profiles: [mspbots]
    image: "${MSP_IMAGE_REPO:-msp-skills}/mspbots:${MSPBOTS_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: mspbots
        CLI_BINARY: mspbots-cli
        MCP_BINARY: mspbots-mcp
        VERSION: "${MSPBOTS_VERSION:-0.1.1}"
    container_name: msp-mspbots
    env_file:
      - env/mspbots.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-mspbots-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  n-central:
    profiles: [n-central]
    image: "${MSP_IMAGE_REPO:-msp-skills}/n-central:${N_CENTRAL_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: n-central
        CLI_BINARY: n-central-cli
        MCP_BINARY: n-central-mcp
        VERSION: "${N_CENTRAL_VERSION:-0.1.1}"
    container_name: msp-n-central
    env_file:
      - env/n-central.env
    environment:
      HOME: /data
    user: "10001:10001"
    volumes:
      - msp-n-central-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  nerdio:
    profiles: [nerdio]
    image: "${MSP_IMAGE_REPO:-msp-skills}/nerdio:${NERDIO_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: nerdio
        CLI_BINARY: nerdio-cli
        MCP_BINARY: nerdio-mcp
        VERSION: "${NERDIO_VERSION:-0.1.1}"
    container_name: msp-nerdio
    env_file:
      - env/nerdio.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-nerdio-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  ninjaone:
    profiles: [ninjaone]
    image: "${MSP_IMAGE_REPO:-msp-skills}/ninjaone:${NINJAONE_VERSION:-0.1.9}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: ninjaone
        CLI_BINARY: ninjaone-cli
        MCP_BINARY: ninjaone-mcp
        VERSION: "${NINJAONE_VERSION:-0.1.9}"
    container_name: msp-ninjaone
    env_file:
      - env/ninjaone.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-ninjaone-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  pagerduty:
    profiles: [pagerduty]
    image: "${MSP_IMAGE_REPO:-msp-skills}/pagerduty:${PAGERDUTY_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: pagerduty
        CLI_BINARY: pagerduty-cli
        MCP_BINARY: pagerduty-mcp
        VERSION: "${PAGERDUTY_VERSION:-0.1.1}"
    container_name: msp-pagerduty
    env_file:
      - env/pagerduty.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-pagerduty-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  pandadoc:
    profiles: [pandadoc]
    image: "${MSP_IMAGE_REPO:-msp-skills}/pandadoc:${PANDADOC_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: pandadoc
        CLI_BINARY: pandadoc-cli
        MCP_BINARY: pandadoc-mcp
        VERSION: "${PANDADOC_VERSION:-0.1.1}"
    container_name: msp-pandadoc
    env_file:
      - env/pandadoc.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-pandadoc-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  pax8:
    profiles: [pax8]
    image: "${MSP_IMAGE_REPO:-msp-skills}/pax8:${PAX8_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: pax8
        CLI_BINARY: pax8-cli
        MCP_BINARY: pax8-mcp
        VERSION: "${PAX8_VERSION:-0.1.1}"
    container_name: msp-pax8
    env_file:
      - env/pax8.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-pax8-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  pipedrive:
    profiles: [pipedrive]
    image: "${MSP_IMAGE_REPO:-msp-skills}/pipedrive:${PIPEDRIVE_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: pipedrive
        CLI_BINARY: pipedrive-cli
        MCP_BINARY: pipedrive-mcp
        VERSION: "${PIPEDRIVE_VERSION:-0.1.1}"
    container_name: msp-pipedrive
    env_file:
      - env/pipedrive.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-pipedrive-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  proofpoint:
    profiles: [proofpoint]
    image: "${MSP_IMAGE_REPO:-msp-skills}/proofpoint:${PROOFPOINT_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: proofpoint
        CLI_BINARY: proofpoint-cli
        MCP_BINARY: proofpoint-mcp
        VERSION: "${PROOFPOINT_VERSION:-0.1.1}"
    container_name: msp-proofpoint
    env_file:
      - env/proofpoint.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-proofpoint-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  quickbooks:
    profiles: [quickbooks]
    image: "${MSP_IMAGE_REPO:-msp-skills}/quickbooks:${QUICKBOOKS_VERSION:-0.1.5}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: quickbooks
        CLI_BINARY: quickbooks-cli
        MCP_BINARY: quickbooks-mcp
        VERSION: "${QUICKBOOKS_VERSION:-0.1.5}"
    container_name: msp-quickbooks
    env_file:
      - env/quickbooks.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-quickbooks-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  resourceguru:
    profiles: [resourceguru]
    image: "${MSP_IMAGE_REPO:-msp-skills}/resourceguru:${RESOURCEGURU_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: resourceguru
        CLI_BINARY: resourceguru-cli
        MCP_BINARY: resourceguru-mcp
        VERSION: "${RESOURCEGURU_VERSION:-0.1.0}"
    container_name: msp-resourceguru
    env_file:
      - env/resourceguru.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-resourceguru-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  rewst:
    profiles: [rewst]
    image: "${MSP_IMAGE_REPO:-msp-skills}/rewst:${REWST_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: rewst
        CLI_BINARY: rewst-cli
        MCP_BINARY: rewst-mcp
        VERSION: "${REWST_VERSION:-0.1.0}"
    container_name: msp-rewst
    env_file:
      - env/rewst.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-rewst-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  rocketcyber:
    profiles: [rocketcyber]
    image: "${MSP_IMAGE_REPO:-msp-skills}/rocketcyber:${ROCKETCYBER_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: rocketcyber
        CLI_BINARY: rocketcyber-cli
        MCP_BINARY: rocketcyber-mcp
        VERSION: "${ROCKETCYBER_VERSION:-0.1.1}"
    container_name: msp-rocketcyber
    env_file:
      - env/rocketcyber.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-rocketcyber-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  rootly:
    profiles: [rootly]
    image: "${MSP_IMAGE_REPO:-msp-skills}/rootly:${ROOTLY_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: rootly
        CLI_BINARY: rootly-cli
        MCP_BINARY: rootly-mcp
        VERSION: "${ROOTLY_VERSION:-0.1.1}"
    container_name: msp-rootly
    env_file:
      - env/rootly.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-rootly-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  runzero:
    profiles: [runzero]
    image: "${MSP_IMAGE_REPO:-msp-skills}/runzero:${RUNZERO_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: runzero
        CLI_BINARY: runzero-cli
        MCP_BINARY: runzero-mcp
        VERSION: "${RUNZERO_VERSION:-0.1.1}"
    container_name: msp-runzero
    env_file:
      - env/runzero.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-runzero-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  salesbuildr:
    profiles: [salesbuildr]
    image: "${MSP_IMAGE_REPO:-msp-skills}/salesbuildr:${SALESBUILDR_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: salesbuildr
        CLI_BINARY: salesbuildr-cli
        MCP_BINARY: salesbuildr-mcp
        VERSION: "${SALESBUILDR_VERSION:-0.1.1}"
    container_name: msp-salesbuildr
    env_file:
      - env/salesbuildr.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-salesbuildr-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  sentinelone:
    profiles: [sentinelone]
    image: "${MSP_IMAGE_REPO:-msp-skills}/sentinelone:${SENTINELONE_VERSION:-0.1.2}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: sentinelone
        CLI_BINARY: sentinelone-cli
        MCP_BINARY: sentinelone-mcp
        VERSION: "${SENTINELONE_VERSION:-0.1.2}"
    container_name: msp-sentinelone
    env_file:
      - env/sentinelone.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-sentinelone-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  servosity:
    profiles: [servosity]
    image: "${MSP_IMAGE_REPO:-msp-skills}/servosity:${SERVOSITY_VERSION:-0.5.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: servosity
        CLI_BINARY: servosity-cli
        MCP_BINARY: servosity-mcp
        VERSION: "${SERVOSITY_VERSION:-0.5.0}"
    container_name: msp-servosity
    env_file:
      - env/servosity.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-servosity-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  sherweb:
    profiles: [sherweb]
    image: "${MSP_IMAGE_REPO:-msp-skills}/sherweb:${SHERWEB_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: sherweb
        CLI_BINARY: sherweb-cli
        MCP_BINARY: sherweb-mcp
        VERSION: "${SHERWEB_VERSION:-0.1.1}"
    container_name: msp-sherweb
    env_file:
      - env/sherweb.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-sherweb-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  skykick:
    profiles: [skykick]
    image: "${MSP_IMAGE_REPO:-msp-skills}/skykick:${SKYKICK_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: skykick
        CLI_BINARY: skykick-cli
        MCP_BINARY: skykick-mcp
        VERSION: "${SKYKICK_VERSION:-0.1.1}"
    container_name: msp-skykick
    env_file:
      - env/skykick.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-skykick-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  superops:
    profiles: [superops]
    image: "${MSP_IMAGE_REPO:-msp-skills}/superops:${SUPEROPS_VERSION:-0.1.4}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: superops
        CLI_BINARY: superops-cli
        MCP_BINARY: superops-mcp
        VERSION: "${SUPEROPS_VERSION:-0.1.4}"
    container_name: msp-superops
    env_file:
      - env/superops.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-superops-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  syncro:
    profiles: [syncro]
    image: "${MSP_IMAGE_REPO:-msp-skills}/syncro:${SYNCRO_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: syncro
        CLI_BINARY: syncro-cli
        MCP_BINARY: syncro-mcp
        VERSION: "${SYNCRO_VERSION:-0.1.1}"
    container_name: msp-syncro
    env_file:
      - env/syncro.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-syncro-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  tactical-rmm:
    profiles: [tactical-rmm]
    image: "${MSP_IMAGE_REPO:-msp-skills}/tactical-rmm:${TACTICAL_RMM_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: tactical-rmm
        CLI_BINARY: tactical-rmm-cli
        MCP_BINARY: tactical-rmm-mcp
        VERSION: "${TACTICAL_RMM_VERSION:-0.1.1}"
    container_name: msp-tactical-rmm
    env_file:
      - env/tactical-rmm.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-tactical-rmm-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  threatlocker:
    profiles: [threatlocker]
    image: "${MSP_IMAGE_REPO:-msp-skills}/threatlocker:${THREATLOCKER_VERSION:-0.3.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: threatlocker
        CLI_BINARY: threatlocker-cli
        MCP_BINARY: threatlocker-mcp
        VERSION: "${THREATLOCKER_VERSION:-0.3.1}"
    container_name: msp-threatlocker
    env_file:
      - env/threatlocker.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-threatlocker-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  unifi-network:
    profiles: [unifi-network]
    image: "${MSP_IMAGE_REPO:-msp-skills}/unifi-network:${UNIFI_NETWORK_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: unifi-network
        CLI_BINARY: unifi-network-cli
        MCP_BINARY: unifi-network-mcp
        VERSION: "${UNIFI_NETWORK_VERSION:-0.1.1}"
    container_name: msp-unifi-network
    env_file:
      - env/unifi-network.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-unifi-network-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  veeam:
    profiles: [veeam]
    image: "${MSP_IMAGE_REPO:-msp-skills}/veeam:${VEEAM_VERSION:-0.1.0}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: veeam
        CLI_BINARY: veeam-cli
        MCP_BINARY: veeam-mcp
        VERSION: "${VEEAM_VERSION:-0.1.0}"
    container_name: msp-veeam
    env_file:
      - env/veeam.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-veeam-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  wordpress:
    profiles: [wordpress]
    image: "${MSP_IMAGE_REPO:-msp-skills}/wordpress:${WORDPRESS_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: wordpress
        CLI_BINARY: wordpress-cli
        MCP_BINARY: wordpress-mcp
        VERSION: "${WORDPRESS_VERSION:-0.1.1}"
    container_name: msp-wordpress
    env_file:
      - env/wordpress.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-wordpress-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  xero:
    profiles: [xero]
    image: "${MSP_IMAGE_REPO:-msp-skills}/xero:${XERO_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: xero
        CLI_BINARY: xero-cli
        MCP_BINARY: xero-mcp
        VERSION: "${XERO_VERSION:-0.1.1}"
    container_name: msp-xero
    env_file:
      - env/xero.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-xero-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

  zammad:
    profiles: [zammad]
    image: "${MSP_IMAGE_REPO:-msp-skills}/zammad:${ZAMMAD_VERSION:-0.1.1}"
    build:
      context: .
      dockerfile: Dockerfile.connector
      args:
        SLUG: zammad
        CLI_BINARY: zammad-cli
        MCP_BINARY: zammad-mcp
        VERSION: "${ZAMMAD_VERSION:-0.1.1}"
    container_name: msp-zammad
    env_file:
      - env/zammad.env
    environment:
      HOME: /data
      PP_MCP_TRANSPORT: http
    user: "10001:10001"
    volumes:
      - msp-zammad-data:/data
    networks: [msp-internal]
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped
    mem_limit: 512m
    pids_limit: 512
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

networks:
  msp-internal:
    driver: bridge
    # Not `internal: true`: connectors must reach their vendor APIs.
    # Isolation comes from publishing no ports, not from blocking egress.

volumes:
  msp-abnormal-data:
  msp-acronis-data:
  msp-action1-data:
  msp-afi-data:
  msp-appdirect-data:
  msp-atera-data:
  msp-autotask-data:
  msp-auvik-data:
  msp-aws-billing-data:
  msp-axcient-data:
  msp-betterstack-data:
  msp-blumira-data:
  msp-cipp-data:
  msp-connectwise-automate-data:
  msp-connectwise-control-data:
  msp-connectwise-manage-data:
  msp-cork-data:
  msp-cove-data:
  msp-crowdstrike-data:
  msp-datto-bcdr-data:
  msp-datto-rmm-data:
  msp-domotz-data:
  msp-gradient-data:
  msp-halopsa-data:
  msp-hubspot-data:
  msp-hudu-data:
  msp-huntress-data:
  msp-immybot-data:
  msp-itglue-data:
  msp-kaseya-bms-data:
  msp-knowbe4-data:
  msp-levelio-data:
  msp-liongard-data:
  msp-maxio-data:
  msp-microsoft-graph-data:
  msp-mspbots-data:
  msp-n-central-data:
  msp-nerdio-data:
  msp-ninjaone-data:
  msp-pagerduty-data:
  msp-pandadoc-data:
  msp-pax8-data:
  msp-pipedrive-data:
  msp-proofpoint-data:
  msp-quickbooks-data:
  msp-resourceguru-data:
  msp-rewst-data:
  msp-rocketcyber-data:
  msp-rootly-data:
  msp-runzero-data:
  msp-salesbuildr-data:
  msp-sentinelone-data:
  msp-servosity-data:
  msp-sherweb-data:
  msp-skykick-data:
  msp-superops-data:
  msp-syncro-data:
  msp-tactical-rmm-data:
  msp-threatlocker-data:
  msp-unifi-network-data:
  msp-veeam-data:
  msp-wordpress-data:
  msp-xero-data:
  msp-zammad-data:
