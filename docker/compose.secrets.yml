# GENERATED by tools/maintainer/build_compose.py - DO NOT EDIT.
# OPTIONAL overlay: credentials live on the Docker host, not here.
#
# Set MSP_SECRETS_DIR in docker/.env to a directory ON THE DOCKER HOST
# holding one <slug>.env per connector, then:
#
# Each file must be readable by uid 10001, which is what the containers
# run as. The host file's own owner and mode apply inside the container,
# so a file owned by your login account at 0600 gives Permission denied:
#
#   chown 10001:10001 <dir>/*.env && chmod 0400 <dir>/*.env
#
# Keep the directory itself 0700 so other host accounts cannot reach in.
#
#
#   docker compose -f compose.yml -f compose.secrets.yml --profile <slug> up -d
#
# The files are mounted read-only and never leave that host. They are
# still visible to anyone with daemon access there, which is the same
# trust boundary the rest of this deployment already assumes.

services:
  abnormal:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/abnormal.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  acronis:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/acronis.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  action1:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/action1.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  afi:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/afi.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  appdirect:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/appdirect.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  atera:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/atera.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  autotask:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/autotask.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  auvik:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/auvik.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  aws-billing:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/aws-billing.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  axcient:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/axcient.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  betterstack:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/betterstack.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  blumira:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/blumira.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  cipp:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/cipp.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  connectwise-automate:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/connectwise-automate.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  connectwise-control:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/connectwise-control.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  connectwise-manage:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/connectwise-manage.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  cork:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/cork.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  cove:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/cove.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  crowdstrike:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/crowdstrike.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  datto-bcdr:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/datto-bcdr.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  datto-rmm:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/datto-rmm.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  domotz:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/domotz.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  gradient:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/gradient.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  halopsa:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/halopsa.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  hubspot:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/hubspot.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  hudu:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/hudu.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  huntress:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/huntress.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  immybot:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/immybot.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  itglue:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/itglue.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  kaseya-bms:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/kaseya-bms.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  knowbe4:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/knowbe4.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  levelio:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/levelio.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  liongard:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/liongard.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  maxio:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/maxio.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  microsoft-graph:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/microsoft-graph.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  mspbots:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/mspbots.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  n-central:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/n-central.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  nerdio:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/nerdio.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  ninjaone:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/ninjaone.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  pagerduty:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/pagerduty.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  pandadoc:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/pandadoc.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  pax8:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/pax8.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  pipedrive:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/pipedrive.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  proofpoint:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/proofpoint.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  quickbooks:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/quickbooks.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  resourceguru:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/resourceguru.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  rewst:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/rewst.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  rocketcyber:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/rocketcyber.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  rootly:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/rootly.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  runzero:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/runzero.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  salesbuildr:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/salesbuildr.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  sentinelone:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/sentinelone.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  servosity:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/servosity.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  sherweb:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/sherweb.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  skykick:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/skykick.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  superops:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/superops.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  syncro:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/syncro.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  tactical-rmm:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/tactical-rmm.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  threatlocker:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/threatlocker.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  unifi-network:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/unifi-network.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  veeam:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/veeam.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  wordpress:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/wordpress.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  xero:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/xero.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
  zammad:
    volumes:
      - ${MSP_SECRETS_DIR:?set MSP_SECRETS_DIR in docker/.env}/zammad.env:/run/secrets/connector.env:ro
    environment:
      MSP_CREDENTIAL_PROVIDER: "cat /run/secrets/connector.env"
