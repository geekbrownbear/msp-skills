# ===== macOS =====
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
.apdisk

# ===== Windows =====
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
*.stackdump

# ===== Linux =====
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ===== Editors and IDEs =====
.vscode/
.idea/
*.swp
*.swo
*.swn
*.bak
*.tmp
.\#*
\#*\#
.project
.classpath
.settings/
nbproject/
*.sublime-workspace
*.sublime-project

# ===== Go =====
# Compiled binaries (we ship via Releases, never check into git)
bin/
build/
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.out
coverage.html
# Skill CLI / MCP binaries (extensionless on macOS/Linux). Catches anything that
# matches the *-cli or *-mcp convention so contributors cannot accidentally
# commit a 20+ MB binary into a skill directory.
skills/*/*-cli
skills/*/*-mcp
skills/*/*-cli-*
skills/*/*-mcp-*
# Locally-built binaries at a vendored Go module root (skills/<skill>/cli/).
# A stray `go build -o <slug>-mcp` at the module root drops a 60+ MB binary one
# level under cli/, which the skills/*/*-mcp rule above does NOT reach. These
# globs catch every connector (incl. the -pp- internal variants) by convention,
# so a new skill can't reintroduce the gap. The `*` cannot cross `/`, so they
# match only cli/<name> and never the tracked cli/cmd/<name>/ source dirs.
# (The Makefile emits to bin/, already ignored; this covers manual builds.)
skills/*/cli/*-cli
skills/*/cli/*-mcp
*.mcpb
# Go workspace
go.work
go.work.sum
# Vendor only if anyone runs `go mod vendor`
vendor/

# ===== Python (uv-based) =====
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
*.egg
.eggs/
.venv/
venv/
env/
ENV/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# ===== Node (for catalog.yml workflow tooling) =====
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm/

# ===== Local databases shipped with HaloPSA + Servosity =====
# Both CLIs cache state in SQLite. We never commit a database file.
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm

# ===== Secrets and config =====
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.pfx
.netrc
.aws/
.config/gcloud/

# ===== Logs =====
*.log
logs/
log/
*.log.*

# ===== Caches and state =====
.cache/
.tmp/
tmp/
.printing-press.json
.printing-press-*.json
.manuscripts/
dogfood-results.json
workflow-verify-report.json
tools-manifest.json
spec.json
# Maintainer-tool advisory locks (release queue serialization). Run-local only.
tools/maintainer/.locks/
# Adversarial-review receipts per skill. Run-local, never committed.
skills/*/.review/

# ===== Test artifacts =====
*.lcov
test-results/
playwright-report/
test-output/

# ===== AI agent local state =====
.claude/
.codex/
.cursor/
.aider*
# Vercel `skills` CLI installs third-party skills (e.g. marketingskills)
# into .agents/ at the project root. Local-only dev tooling, never committed.
.agents/
skills-lock.json

# ===== Misc =====
*.zip
*.tar
*.tar.gz
*.7z
*.iso
*.dmg
.history/
# Glob the children rather than the directory: git cannot re-include a file
# whose parent directory is excluded, and cards.yaml below needs re-including.
assets/social/*
# ...except the per-skill card content. It is the source the social-card
# generator reads for each skill's terminal prompt, so it has to be reviewable
# and reproducible, not a local scratch file. Everything else under
# assets/social/ (AI backgrounds, _variants showcases, ab-log) stays ignored.
!assets/social/cards.yaml
.gstack/

# Docker deployment: operator-owned files holding real credentials and pins.
# The committed *.example files are generated by tools/maintainer/build_compose.py.
#
# The `env/` rule above excludes any directory of that name, and git will not
# re-include a file inside an excluded directory, so the directory has to be
# un-ignored before the negation below can take effect.
!docker/env/
docker/.env
docker/env/*.env
!docker/env/*.env.example
