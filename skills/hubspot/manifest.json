{
  "manifest_version": "0.3",
  "name": "hubspot-mcp",
  "display_name": "HubSpot",
  "version": "0.1.3",
  "description": "Every HubSpot Sales Hub feature, plus offline cross-object queries, property-change-history reporting, and an agent-native data layer no other HubSpot tool has.",
  "author": {
    "name": "Servosity Inc."
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/hubspot-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/hubspot-mcp",
      "args": [],
      "env": {
        "HUBSPOT_ACCESS_TOKEN": "${user_config.hubspot_access_token}",
        "HUBSPOT_BASE_URL": "${user_config.hubspot_base_url}",
        "HUBSPOT_OWNER_EMAIL": "${user_config.hubspot_owner_email}"
      }
    }
  },
  "user_config": {
    "hubspot_access_token": {
      "type": "string",
      "title": "HUBSPOT_ACCESS_TOKEN",
      "description": "Sets HUBSPOT_ACCESS_TOKEN for the HubSpot MCP server. Get a credential from https://app.hubspot.com/private-apps.",
      "sensitive": true,
      "required": true
    },
    "hubspot_base_url": {
      "type": "string",
      "title": "HUBSPOT_BASE_URL",
      "description": "Base URL of your HubSpot instance",
      "required": false,
      "default": "https://api.hubapi.com"
    },
    "hubspot_owner_email": {
      "type": "string",
      "title": "HUBSPOT_OWNER_EMAIL",
      "description": "HUBSPOT_OWNER_EMAIL for HubSpot",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  },
  "printing_press_version": "4.24.0",
  "press_provenance": {
    "printing_press_version": "4.24.0",
    "generated_at": "2026-06-13T04:36:42.540934Z",
    "spec_checksum": null,
    "spec_format": null
  }
}
