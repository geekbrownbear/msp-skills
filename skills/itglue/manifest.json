{
  "manifest_version": "0.3",
  "name": "itglue-mcp",
  "display_name": "IT Glue / MyGlue",
  "version": "0.1.1",
  "description": "Every IT Glue resource, plus an offline SQLite mirror, fleet-wide cross-resource search, and documentation-hygiene analytics no other IT Glue tool offers.",
  "author": {
    "name": "Servosity Inc."
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/itglue-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/itglue-mcp",
      "args": [],
      "env": {
        "ITGLUE_API_KEY": "${user_config.itglue_api_key}",
        "ITGLUE_BASE_URL": "${user_config.itglue_base_url}"
      }
    }
  },
  "user_config": {
    "itglue_api_key": {
      "type": "string",
      "title": "ITGLUE_API_KEY",
      "description": "IT Glue API key with permissions for the requested read and write operations.",
      "sensitive": true,
      "required": true
    },
    "itglue_base_url": {
      "type": "string",
      "title": "ITGLUE_BASE_URL",
      "description": "Base URL of your IT Glue instance",
      "required": false,
      "default": "https://api.itglue.com"
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  },
  "printing_press_version": "4.24.0",
  "press_provenance": {
    "printing_press_version": "4.24.0",
    "generated_at": "2026-06-13T03:47:46.666489Z",
    "spec_checksum": null,
    "spec_format": null
  }
}
