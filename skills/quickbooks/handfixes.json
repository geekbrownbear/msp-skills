{
  "slug": "quickbooks",
  "doc": "docs/reprint-survival.md",
  "_comment": "Connector-specific edits to GENERATED files that a cli-printing-press reprint must NOT clobber. check_handfixes.py asserts every marker below is still present and fails CI if a reprint dropped one. status: active = connector-only, a reprint would clobber it; upstreamed = also fixed in the press but the back-port must still be present today.",
  "handfixes": [
    {
      "id": "qbo-query-injection",
      "summary": "sync injects `select * from <Entity>` (+ minorversion) for the /query endpoint; QBO has no plain REST list path",
      "why": "Every QuickBooks Online entity is read through /query, which REQUIRES a SQL-like `query` parameter. The generated sync path sent a bare /query with no query text, and QBO answers HTTP 200 with a `{\"Fault\":...SystemFailureError}` envelope (not a 4xx). The generic extractor finds no array, so upsertSingleObject gets the Fault envelope, extractObjectID returns \"\", and every resource fails with `missing id for <resource>` (0 rows). Fix injects a per-entity `select * from <Entity> ...` and `minorversion=75` when path==\"/query\", mapping each sync resource to its PascalCase QBO entity.",
      "status": "active",
      "spec_encode_followup": "Press change: teach cli-printing-press to inject the entity SQL + minorversion for QBO-shaped /query endpoints via a sync-hint / x-pp-query spec extension, so a reprint regenerates this by construction. File via /printing-press-retro.",
      "asserts": [
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "quickbooksEntity",
          "min_count": 2
        },
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "\"minorversion\"",
          "min_count": 1
        },
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "\"JournalEntry\"",
          "min_count": 1
        }
      ]
    },
    {
      "id": "qbo-query-paging",
      "summary": "sync pages QBO /query via in-query STARTPOSITION/MAXRESULTS until a short page; without it the mirror truncates at 1000 rows/entity",
      "why": "resourceSupportsPagination() is false for every QBO entity, so the generic sync loop breaks after page 1 - capping each entity at one 1000-row page (QBO's MAXRESULTS hard cap). On a real book this silently truncated catastrophically: only the first 1000 rows of each entity, making ar-aging/ap-aging/dso/balances wrong (not just partial), and QBO returns rows oldest-first so recent invoices were missing entirely. Fix carries the next STARTPOSITION in the loop's `cursor`, sets hasMore from full-page detection (len(items) >= qboMaxResults), and exempts /query from the generic !resourceSupportsPagination and short-page breaks. Verified live against a production tenant: all 8 resources sync to their exact live count(*) across the full book.",
      "status": "active",
      "spec_encode_followup": "Same press change as qbo-query-injection: a QBO sync-hint that emits STARTPOSITION/MAXRESULTS paging by construction.",
      "asserts": [
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "qboMaxResults",
          "min_count": 2
        },
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "startposition %d maxresults %d",
          "min_count": 1
        },
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "path != \"/query\"",
          "min_count": 2
        }
      ]
    },
    {
      "id": "qbo-queryresponse-envelope",
      "summary": "page-item extraction unwraps QBO's two-level `{\"QueryResponse\":{\"<Entity>\":[...]}}` envelope",
      "why": "QBO /query nests the result array one level deeper than the generic extractor handles: `QueryResponse` (object) then the PascalCase entity key (array). Without `QueryResponse` in dataEnvelopeKeys, extractPageItems cannot reach the entity array, returns 0 items, and sync falls through to single-object upsert of the whole envelope. Adding it lets the existing two-level unwrap + single-array-sibling logic pull the entity array correctly. Paired with qbo-query-injection; both are required for sync to store any rows.",
      "status": "active",
      "spec_encode_followup": "Same press change as qbo-query-injection: a QBO response-envelope hint so the generated extractor unwraps QueryResponse.<Entity> by construction.",
      "asserts": [
        {
          "file": "cli/internal/cli/sync.go",
          "contains": "\"QueryResponse\"",
          "min_count": 1
        }
      ]
    },
    {
      "id": "qbo-db-path-env",
      "summary": "defaultDBPath honors QUICKBOOKS_DB_PATH so sandbox and production keep separate local mirrors",
      "why": "The local SQLite mirror path was hardcoded to ~/.local/share/quickbooks-cli/data.db with no per-tenant separation. QBO entity IDs are per-company and collide across companies (sandbox Id \"58\" vs production Id \"58\"), so running both sandbox and production against one data.db corrupts aggregates after a mode switch. The Keychain wrapper sets QUICKBOOKS_DB_PATH per mode (.../sandbox/data.db vs .../production/data.db); this env override makes every command (sync, doctor, the aggregates) honor it.",
      "status": "active",
      "spec_encode_followup": "Press change: a generic <CLI>_DB_PATH env override on defaultDBPath would let any multi-tenant connector isolate mirrors without a hand-edit.",
      "asserts": [
        {
          "file": "cli/internal/cli/helpers.go",
          "contains": "QUICKBOOKS_DB_PATH",
          "min_count": 1
        }
      ]
    },
    {
      "id": "qbo-aging-snapshots-table",
      "summary": "aging-delta creates its aging_snapshots table on demand; no store migration defines it",
      "why": "aging_delta.go (a novel hand-authored command) reads and writes an aging_snapshots table, but nothing in the store migrations creates it. So every aging-delta run errored `no such table: aging_snapshots` on the read path (loadLatestAgingSnapshot treats only sql.ErrNoRows as a clean first-run; a missing table is a hard error) - the command was broken from first use. Fix adds ensureAgingSnapshotsTable (CREATE TABLE IF NOT EXISTS) called before the read, so a fresh or freshly reprinted DB self-heals. aging_delta.go is a novel file kept whole by regen-merge, so this survives a reprint; this entry is the cheap backstop the reprint-survival doc recommends for novel commands.",
      "status": "active",
      "spec_encode_followup": "Press change: novel commands that own a private table should declare it in the store migration set so it is created by construction.",
      "asserts": [
        {
          "file": "cli/internal/cli/aging_delta.go",
          "contains": "CREATE TABLE IF NOT EXISTS aging_snapshots",
          "min_count": 1
        }
      ]
    },
    {
      "id": "qbo-report-reports-api",
      "summary": "report pnl + report balance-sheet pull QBO financial statements (P&L, Balance Sheet) from the Reports API, which the /query-based CLI cannot reach",
      "why": "QuickBooks financial statements live behind the Reports API (/reports/ProfitAndLoss, /reports/BalanceSheet), not /query, so the generated CLI could not produce them. report.go is a novel hand-authored command group (report pnl, report balance-sheet; read-only) kept whole by regen-merge, but its registration in the generated root.go (rootCmd.AddCommand(newReportCmd(flags))) is a hand-edit a reprint would clobber. This entry records it so the command survives.",
      "status": "active",
      "spec_encode_followup": "Press change: encode the report command group in the QBO spec (or a novel-feature manifest) so root.go registers it by construction without a hand-edit.",
      "asserts": [
        {
          "file": "cli/internal/cli/root.go",
          "contains": "newReportCmd(flags)",
          "min_count": 1
        },
        {
          "file": "cli/internal/cli/report.go",
          "contains": "/reports/ProfitAndLoss",
          "min_count": 1
        },
        {
          "file": "cli/internal/cli/report.go",
          "contains": "/reports/BalanceSheet",
          "min_count": 1
        }
      ]
    },
    {
      "id": "oauth-env-and-auto-refresh",
      "summary": "QUICKBOOKS_CLIENT_ID/SECRET/REFRESH_TOKEN configure refresh by environment, the client refreshes expired tokens itself, and rotations persist",
      "why": "TokenExpiry was written by SaveTokens and read by nothing, `auth refresh` existed and nothing called it, and an Intuit access token lives one hour, so the connector worked for an hour per manual refresh. Same defect cipp had, fixed in the same shape: ensureFreshToken in the request path, mutex-guarded because the MCP server serves tools concurrently, verify-mode never dials. Verified live: with a fake refresh trio the mint reached Intuit's token endpoint and surfaced invalid_client; with a static QUICKBOOKS_ACCESS_TOKEN no mint happens and the request goes straight to the API. One deliberate difference from cipp: the refreshed material PERSISTS via SaveTokens. Intuit rotates refresh tokens and invalidates the previous value, so an in-memory-only rotation locks the connector out on the next restart with no recovery short of redoing the browser OAuth flow. SaveTokens also previously deleted the ClientID and ClientSecret env-override markers before saving, which defeated configForSave's guard and wrote an env-supplied client secret to disk in cleartext; that is the same defect immybot's save-tokens-keeps-env-credential-markers records. It now keeps those markers, and configForSave restores ClientID, ClientSecret and RefreshToken from the file snapshot when their live values came from the environment. Verified: a save with the trio set in the environment leaves all three empty on disk while the minted access token, rotated refresh token and expiry persist. doctor also reported not configured when the refresh trio was present but no token had been minted yet.",
      "status": "active",
      "spec_encode_followup": "The press auth template should emit the env trio, the expiry-aware request-path refresh, and rotation persistence for any vendor whose refresh tokens rotate; audit which other connectors persist a TokenExpiry nothing reads.",
      "asserts": [
        {
          "file": "cli/internal/config/config.go",
          "contains": "QUICKBOOKS_REFRESH_TOKEN",
          "min_count": 1
        },
        {
          "file": "cli/internal/config/config.go",
          "contains": "func (c *Config) TokenNeedsRefresh()",
          "min_count": 1
        },
        {
          "file": "cli/internal/client/client.go",
          "contains": "func (c *Client) ensureFreshToken(",
          "min_count": 1
        },
        {
          "file": "cli/internal/client/oauth.go",
          "contains": "func RefreshAccessToken(",
          "min_count": 1
        },
        {
          "file": "cli/internal/cli/doctor.go",
          "contains": "cfg.CanMintToken()",
          "min_count": 1
        }
      ]
    }
  ]
}
